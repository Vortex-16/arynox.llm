import React from 'react';

const Features: React.FC = () => {
  return (
    <section 
      className="relative w-full h-full bg-[#20B2AA]"
      id="features"
    />
  );
};

export default Features;
